services:
  - type: web
    name: payment-approval-app
    env: node
    buildCommand: |
      npm install
      npm run build
    startCommand: node server.js
    staticPublishPath: /opt/render/project/src/dist
    envVars:
      - key: NODE_ENV
        value: production
